/**
 * Supabase schema placeholder.
 *
 * The project should ideally import generated types from Supabase (e.g. via `npm run gen:types`),
 * but until the OpenAPI schema is available at build time we fall back to `any` so that the
 * application code can compile. Once the schema file exists, replace this with the generated type.
 */
export type SupabaseDatabase = any;
